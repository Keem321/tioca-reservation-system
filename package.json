{
	"name": "tioca-reservation-system-root",
	"private": true,
	"scripts": {
		"dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
	},
	"devDependencies": {
		"concurrently": "^8.2.2"
	},
	"dependencies": {
		"express-session": "^1.18.2",
		"lucide-react": "^0.562.0",
		"passport-google-oauth20": "^2.0.0",
		"passport-local": "^1.0.0"
	}
}
